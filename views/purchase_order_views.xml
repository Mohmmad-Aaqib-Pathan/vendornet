<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_vn_po_form" model="ir.ui.view">
            <field name="name">vn.purchase.order.form</field>
            <field name="model">vn.purchase.order</field>
            <field name="arch" type="xml">
                <form string="Purchase Order">
                    <header>
                        <button name="action_confirm" type="object" string="Confirm PO"
                            class="btn-primary" invisible="state != 'draft'"/>
                        <button name="action_create_invoice" type="object" string="Create Invoice"
                            class="btn-success" invisible="state != 'confirmed'"/>
                        <button name="action_done" type="object" string="Mark Done"
                            invisible="state != 'confirmed'"/>
                        <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('done','cancelled')"/>
                        <field name="state" widget="statusbar"
                            statusbar_visible="draft,confirmed,done"/>
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button type="object" name="action_view_invoices"
                                class="oe_stat_button" icon="fa-pencil-square-o"
                                invisible="invoice_count == 0">
                                <field name="invoice_count" widget="statinfo" string="Invoices"/>
                            </button>
                        </div>
                        <div class="oe_title">
                            <h1><field name="name" readonly="1"/></h1>
                        </div>
                        <group>
                            <group>
                                <field name="vendor_id"/>
                                <field name="rfq_id"/>
                                <field name="quotation_id"/>
                            </group>
                            <group>
                                <field name="order_date"/>
                                <field name="delivery_date"/>
                                <field name="amount_total"/>
                            </group>
                        </group>
                        <group string="Notes">
                            <field name="notes" nolabel="1"/>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="view_vn_po_list" model="ir.ui.view">
            <field name="name">vn.purchase.order.list</field>
            <field name="model">vn.purchase.order</field>
            <field name="arch" type="xml">
                <list string="Purchase Orders"
                    decoration-success="state == 'confirmed'"
                    decoration-muted="state == 'done'">
                    <field name="name"/>
                    <field name="vendor_id"/>
                    <field name="order_date"/>
                    <field name="delivery_date"/>
                    <field name="amount_total"/>
                    <field name="state"/>
                </list>
            </field>
        </record>

        <record id="action_vn_po" model="ir.actions.act_window">
            <field name="name">Purchase Orders</field>
            <field name="res_model">vn.purchase.order</field>
            <field name="view_mode">list,form</field>
        </record>
    </data>
</odoo>